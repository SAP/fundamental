{% extends "layout.njk" %}
{% import "./../utils.njk" as utils %}
{% from "./../format.njk" import format %}
{% from "./../popover/component.njk" import popover %}
{% from "./../menu/component.njk" import menu %}
{% from "../button/component.njk" import button %}
{% import "../forms.njk" as forms %}
{% from "./component.njk" import localization_editor %}

<!-- include add'tl css from src/styles/, e.g., ['helpers','components/button'] -->
{% set css_deps = ['fonts','helpers', 'components/form', 'components/input', 'components/menu', 'components/popover','components/button','components/input-group', 'icons'] %}


{% block content %}

    <h1>localization-editor with input</h1>

{% set example %}
{%- set _id = utils.id() %}
{{  localization_editor(
        properties=data.properties
    )
}}

{% endset %}
{{ format(example) }}

    <br><br><br>

    <h1>localization-editor with textarea</h1>

    {% set example %}
    {%- set _id = utils.id() %}
    {{  localization_editor(
            properties={
                type: "input",
                id: _id
            },
            modifier={
                block: []
            },
            state={},
            aria={}
        )
    }}
    {% endset %}
    {{ format(example) }}


{% endblock %}
