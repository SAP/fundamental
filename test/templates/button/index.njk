{% extends "layout.njk" %}
{% from "./../format.njk" import format %}
{% from "./component.njk" import button %}
{% set css_deps = ['fonts','helpers','icons'] %}

{% block content %}
<style media="screen">
  button {
    margin-bottom: 4px !important;
    margin-right: 4px !important;
  }
</style>


Primary
<button class="fd-button sap-icon--save">Button</button>
<button class="fd-button">Button</button>
<button class="fd-button fd-button--positive sap-icon--accept">Approve</button>
<button class="fd-button fd-button--negative sap-icon--decline">Reject</button><br>

Main
<button class="fd-button--main sap-icon--save">Button</button>
<button class="fd-button--main">Button</button>
<button class="fd-button--main fd-button--positive sap-icon--accept">Approve</button>
<button class="fd-button--main fd-button--negative sap-icon--decline">Reject</button><br>

Secondary
<button class="fd-button--secondary sap-icon--save">Button</button>
<button class="fd-button--secondary">Button</button><br>

Toolbar
<button class="fd-button--toolbar sap-icon--save">Button</button>
<button class="fd-button--toolbar">Button</button><br>



<br>
<hr>
<br>


<h1>Button</h1>
<p>Default button</p>
{% set example %}
{{  button(
        {
            label: 'Button Label'
        },
        modifier={
            block: ['xs']
        })
}}
{{  button(
        {
            label: 'Button Label'
        },
        modifier={
            block: ['s']
        })
}}
{{  button(
        {
            label: 'Button Label'
        },
        modifier={
          block: ['compact']
        })
}}
{{  button(
        {
            label: 'Button Label'
        },
        modifier={

        })
}}
{{  button(
        {
            label: 'Button Label'
        },
        modifier={
            block: ['l']
        })
}}
<br>
{{  button(
        {
            label: 'Accept',
            icon: 'accept'
        },
        modifier={
            block: ['xs']
        })
}}
{{  button(
        {
            label: 'Accept',
            icon: 'accept'
        },
        modifier={
            block: ['s']
        })
}}
{{  button(
        {
            label: 'Accept',
            icon: 'accept'
        },
        modifier={
          block: ['compact']
        })
}}
{{  button(
        {
            label: 'Accept',
            icon: 'accept'
        },
        modifier={

        })
}}
{{  button(
        {
            label: 'Accept',
            icon: 'accept'
        },
        modifier={
            block: ['l']
        })
}}
<br>
{{  button(
        {
            icon: 'add-favorite'
        },
        modifier={
            block: ['xs']
        },
        aria={ label: 'Add favorite' })
}}
{{  button(
        {
            icon: 'add-favorite'
        },
        modifier={
            block: ['s']
        },
        aria={ label: 'Add favorite' })
}}
{{  button(
        {
            icon: 'add-favorite'
        },
        modifier={
          block: ['compact']
        },
        aria={ label: 'Add favorite' })
}}
{{  button(
        {
            icon: 'add-favorite'
        },
        modifier={

        },
        aria={ label: 'Add favorite' })
}}
{{  button(
        {
            icon: 'add-favorite'
        },
        modifier={
            block: ['l']
        },
        aria={ label: 'Add favorite' })
}}
<br>
{{  button(
        { label: 'Selected' },
        state={ selected: true }
    )
}}
{{  button(
        { label: 'Selected' },
        aria={ selected: true }
    )
}}

{{  button(
        { label: 'Disabled' },
        state={ disabled: true }
    )
}}
{{  button(
        { label: 'Disabled' },
        aria={ disabled: true }
    )
}}

<br>
{{  button(
        {
            label: 'Approve',
            icon: 'accept'
        },
        modifier={
            block: ['xs','positive']
        })
}}
{{  button(
        {
            label: 'Approve',
            icon: 'accept'
        },
        modifier={
            block: ['s','positive']
        })
}}
{{  button(
        {
            label: 'Approve',
            icon: 'accept'
        },
        modifier={
          block: ['compact','positive']
        })
}}
{{  button(
        {
            label: 'Approve',
            icon: 'accept'
        },
        modifier={
block: ['positive']
        })
}}
{{  button(
        {
            label: 'Approve',
            icon: 'accept'
        },
        modifier={
            block: ['l','positive']
        })
}}
<br>
{{  button(
        {
            label: 'Reject',
            icon: 'decline'
        },
        modifier={
            block: ['xs','negative']
        })
}}
{{  button(
        {
          label: 'Reject',
          icon: 'decline'
        },
        modifier={
            block: ['s','negative']
        })
}}
{{  button(
        {
          label: 'Reject',
          icon: 'decline'
        },
        modifier={
          block: ['compact','negative']
        })
}}
{{  button(
        {
          label: 'Reject',
          icon: 'decline'
        },
        modifier={
block: ['negative']
        })
}}
{{  button(
        {
          label: 'Reject',
          icon: 'decline'
        },
        modifier={
            block: ['l','negative']
        })
}}



{% endset %}
{{ format(example) }}

<br><br>


<h1>Main Button</h1>
<p>The main action, only one should be on a page</p>
{% set example %}
{{  button(
        { label: 'Button' },
        modifier={ block: ['main','xs'] })
}}
{{  button(
        { label: 'Button' },
        modifier={ block: ['main','s'] })
}}
{{  button(
        { label: 'Button' },
        modifier={ block: ['main','compact'] })
}}
{{  button(
        { label: 'Button' },
        modifier={ block: ['main'] })
}}
{{  button(
        { label: 'Button' },
        modifier={ block: ['main','l'] })
}}
<br>
{{  button(
        { label: 'Add to Cart',
          icon: 'cart' },
        modifier={ block: ['main','xs'] })
}}
{{  button(
        { label: 'Add to Cart',
          icon: 'cart' },
        modifier={ block: ['main','s'] })
}}
{{  button(
  { label: 'Add to Cart',
    icon: 'cart' },
        modifier={ block: ['main','compact'] })
}}
{{  button(
  { label: 'Add to Cart',
    icon: 'cart' },
        modifier={ block: ['main'] })
}}
{{  button(
  { label: 'Add to Cart',
    icon: 'cart' },
        modifier={ block: ['main','l'] })
}}
<br>
{{  button(
        {
          icon: 'attachment' },
        modifier={ block: ['main','xs'] },
        aria={ label: 'Add Attachment' })
}}
{{  button(
        {  icon: 'attachment' },
        modifier={ block: ['main','s'] },
        aria={ label: 'Add Attachment' })
}}
{{  button(
  {
    icon: 'attachment' },
        modifier={ block: ['main','compact'] },
        aria={ label: 'Add Attachment' })
}}
{{  button(
  {
    icon: 'attachment' },
        modifier={ block: ['main'] },
        aria={ label: 'Add Attachment' })
}}
{{  button(
  {
    icon: 'attachment' },
        modifier={ block: ['main','l'] },
        aria={ label: 'Add Attachment' })
}}
<br>
{{  button(
        { label: 'Selected' },
        modifier={ block: ['main'] },
        state={ selected: true }
    )
}}
{{  button(
  { label: 'Selected' },
  modifier={ block: ['main'] },
        aria={ selected: true }
    )
}}

{{  button(
  { label: 'Disabled' },
  modifier={ block: ['main'] },
        state={ disabled: true }
    )
}}
{{  button(
  { label: 'Disabled' },
  modifier={ block: ['main'] },
        aria={ disabled: true }
    )
}}



<br>
{{  button(
        {
            label: 'Approve',
            icon: 'accept'
        },
        modifier={
            block: ['xs','main','positive']
        })
}}
{{  button(
        {
            label: 'Approve',
            icon: 'accept'
        },
        modifier={
            block: ['s','main','positive']
        })
}}
{{  button(
        {
            label: 'Approve',
            icon: 'accept'
        },
        modifier={
          block: ['compact','main','positive']
        })
}}
{{  button(
        {
            label: 'Approve',
            icon: 'accept'
        },
        modifier={
block: ['main','positive']
        })
}}
{{  button(
        {
            label: 'Approve',
            icon: 'accept'
        },
        modifier={
            block: ['l','main','positive']
        })
}}
<br>
{{  button(
        {
            label: 'Reject',
            icon: 'decline'
        },
        modifier={
            block: ['xs','main','negative']
        })
}}
{{  button(
        {
          label: 'Reject',
          icon: 'decline'
        },
        modifier={
            block: ['s','main','negative']
        })
}}
{{  button(
        {
          label: 'Reject',
          icon: 'decline'
        },
        modifier={
          block: ['compact','main','negative']
        })
}}
{{  button(
        {
          label: 'Reject',
          icon: 'decline'
        },
        modifier={
block: ['main','negative']
        })
}}
{{  button(
        {
          label: 'Reject',
          icon: 'decline'
        },
        modifier={
            block: ['l','main','negative']
        })
}}

{% endset %}
{{ format(example) }}




<br><br>

<h1>Secondary Button</h1>
{% set example %}

{{  button(
        { label: 'Button' },
        modifier={ block: ['secondary','xs'] })
}}
{{  button(
        { label: 'Button' },
        modifier={ block: ['secondary','s'] })
}}
{{  button(
        { label: 'Button' },
        modifier={ block: ['secondary','compact'] })
}}
{{  button(
        { label: 'Button' },
        modifier={ block: ['secondary'] })
}}
{{  button(
        { label: 'Button' },
        modifier={ block: ['secondary','l'] })
}}
<br>
{{  button(
        { label: 'Cancel',
          icon: 'cancel' },
        modifier={ block: ['secondary','xs'] })
}}
{{  button(
  { label: 'Cancel',
    icon: 'cancel' },
        modifier={ block: ['secondary','s'] })
}}
{{  button(
  { label: 'Cancel',
    icon: 'cancel' },
        modifier={ block: ['secondary','compact'] })
}}
{{  button(
  { label: 'Cancel',
    icon: 'cancel' },
        modifier={ block: ['secondary'] })
}}
{{  button(
  { label: 'Cancel',
    icon: 'cancel' },
        modifier={ block: ['secondary','l'] })
}}
<br>
{{  button(
        {
          icon: 'edit' },
        modifier={ block: ['secondary','xs'] },
        aria={ label: 'Edit' })
}}
{{  button(
        {  icon: 'edit' },
        modifier={ block: ['secondary','s'] },
        aria={ label: 'Edit' })
}}
{{  button(
  {
    icon: 'edit' },
        modifier={ block: ['secondary','compact'] },
        aria={ label: 'Edit' })
}}
{{  button(
  {
    icon: 'edit' },
        modifier={ block: ['secondary'] },
        aria={ label: 'Edit' })
}}
{{  button(
  {
    icon: 'edit' },
        modifier={ block: ['secondary','l'] },
        aria={ label: 'Edit' })
}}
<br>
{{  button(
        { label: 'Selected' },
        modifier={ block: ['secondary'] },
        state={ selected: true }
    )
}}
{{  button(
  { label: 'Selected' },
  modifier={ block: ['secondary'] },
        aria={ selected: true }
    )
}}

{{  button(
  { label: 'Disabled' },
  modifier={ block: ['secondary'] },
        state={ disabled: true }
    )
}}
{{  button(
  { label: 'Disabled' },
  modifier={ block: ['secondary'] },
        aria={ disabled: true }
    )
}}

{% endset %}
{{ format(example) }}




<br><br>

<h1>Toolbar Button</h1>
{% set example %}

{{  button(
        { label: 'Button' },
        modifier={ block: ['toolbar','xs'] })
}}
{{  button(
        { label: 'Button' },
        modifier={ block: ['toolbar','s'] })
}}
{{  button(
        { label: 'Button' },
        modifier={ block: ['toolbar','compact'] })
}}
{{  button(
        { label: 'Button' },
        modifier={ block: ['toolbar'] })
}}
{{  button(
        { label: 'Button' },
        modifier={ block: ['toolbar','l'] })
}}
<br>
{{  button(
  { label: 'Sort',
    icon: 'alphabetical-order' },
        modifier={ block: ['toolbar','xs'] })
}}
{{  button(
  { label: 'Sort',
    icon: 'alphabetical-order' },
        modifier={ block: ['toolbar','s'] })
}}
{{  button(
  { label: 'Sort',
    icon: 'alphabetical-order' },
        modifier={ block: ['toolbar','compact'] })
}}
{{  button(
  { label: 'Sort',
    icon: 'alphabetical-order' },
        modifier={ block: ['toolbar'] })
}}
{{  button(
  { label: 'Sort',
    icon: 'alphabetical-order' },
        modifier={ block: ['toolbar','l'] })
}}
<br>
{{  button(
        {
          icon: 'filter' },
        modifier={ block: ['toolbar','xs'] },
        aria={ label: 'Filter' })
}}
{{  button(
        {  icon: 'filter' },
        modifier={ block: ['toolbar','s'] },
        aria={ label: 'Filter' })
}}
{{  button(
  {
    icon: 'filter' },
        modifier={ block: ['toolbar','compact'] },
        aria={ label: 'Filter' })
}}
{{  button(
  {
    icon: 'filter' },
        modifier={ block: ['toolbar'] },
        aria={ label: 'Filter' })
}}
{{  button(
  {
    icon: 'filter' },
        modifier={ block: ['toolbar','l'] },
        aria={ label: 'Filter' })
}}
<br>
{{  button(
        { label: 'Selected' },
        modifier={ block: ['toolbar'] },
        state={ selected: true }
    )
}}
{{  button(
  { label: 'Selected' },
  modifier={ block: ['toolbar'] },
        aria={ selected: true }
    )
}}

{{  button(
  { label: 'Disabled' },
  modifier={ block: ['toolbar'] },
        state={ disabled: true }
    )
}}
{{  button(
  { label: 'Disabled' },
  modifier={ block: ['toolbar'] },
        aria={ disabled: true }
    )
}}

{% endset %}
{{ format(example) }}



{% endblock %}
