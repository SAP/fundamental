{% extends "layout.njk" %}
{% from "./../format.njk" import format %}
{% from "./component.njk" import popover %}

<!-- include add'tl css from src/styles/, e.g., ['helpers','components/button'] -->
{% set css_deps = ['fonts','components/button','icons','components/image'] %}

{% block content %}

<h2>popover</h2>

{% set example %}
  {{  popover(properties={
    items: [
    { "label": "Option 1" },
    { "label": "Option 2" },
    { "label": "Option 3" }
    ] })
  }}
{% endset %}
{{ format(example) }}

{% endblock %}
