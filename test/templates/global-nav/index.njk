{% extends "layout.njk" %}
{% import "./../utils.njk" as utils %}
{% from "./../format.njk" import format %}
{% from "../button/component.njk" import button %}
{% from "../dropdown/component.njk" import dropdown, menu %}
{% from "../nav/component.njk" import nav %}
{% from "./component.njk" import global_nav %}

<!-- include add'tl css from src/styles/, e.g., ['helpers','components/button'] -->
{% set css_deps = ['icons', 'components/button', 'components/dropdown', 'components/nav', 'helpers'] %}

{% block content %}

<div style="background-color: #f3f4f5; margin:-20px; padding:20px;">

    <h1>global-nav</h1>

    <!-- output the component example and the code snippet -->
    {% set example %}
    
    {# {{  global_nav(
            properties={
                "launchpad": {
                    "label": "Home",
                    "icon": "caution",
                    "items": [
                        { "label": "Option 1" },
                        { "label": "Option 2" },
                        { "label": "Option 3" }
                    ]
                },
                "contextMenu": {
                    "label": "Context Name",
                    "icon": "",
                    "items": [
                        { "label": "Option 1" },
                        { "label": "Option 2" },
                        { "label": "Option 3" }
                    ]
                },
                "subNav": {
                    "items": [
                            { "label": "Link text" },
                            { "label": "Selected link", "selected": true },
                            { "label": "Link text" },
                            { "label": "Link text" },
                            { "label": "Link text" },
                            { "label": "Link text" }
                        ]
                    }
            },
            modifier={
                block: data.properties
            },
            state=data.state,
            aria=data.aria
        )
    }} #}

    {{  global_nav(
            properties= data.properties,
            modifier={
                block: data.properties
            },
            state=data.state,
            aria=data.aria
        )
    }}

    {% endset %}
    {{ format(example) }}


{% endblock %}

</div>
