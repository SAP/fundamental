<!--
product_tile:
    properties={},
    modifier={ block: [] },
    state={},
    aria={}
-->
{% macro product_tile(properties={}, modifier={}, state={}, aria={}) -%}
<div class="fd-product-tile{{ modifier.block | modifier('tile') }}{{ state | state }}"{{ aria | aria }}>
{{- caller() | indent -}}
</div>
{%- endmacro %}

{%- macro product_tile_content(properties={}, modifier=[], aria={}, classes=[]) %}
<div class="fd-product-tile__content">
  <h2 class="fd-product-tile__title">{{ properties.title }}</h2>
  {%- if properties.description %}
  <p>{{ properties.description }}</p>
  {%- endif %}
  {{- caller() }}
</div>
{%- endmacro %}

{%- macro product_tile_media(properties={}, modifier=[], aria={}, classes=[]) %}
<div class="fd-product-tile__media" style="background-image: url('{{ properties.url }}');">{{ caller() }}</div>
{%- endmacro %}
