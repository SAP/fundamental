<!--
table:
    properties={},
    modifier={ block: [] },
    state={},
    aria={}
-->
{% macro table(properties={}, modifier={}, state={}, aria={}) -%}

{{ state.sortable | dump(2)}}

<table class="fd-table{{ modifier.block | modifier('table') }}{{ state | state }}{{ classes | classes }}"{{ aria | aria }}>
    <thead>
        <tr>
            {%- for header in properties.headers %}
            {%- set btn_modifier_class = " fd-button--"+modifier.block if modifier.block and modifier.block !== "default" else "" %}
            {% set sort_class = "fd-table__sort-column" if properties.sortable else "" %}
            <th class="{{sort_class}}">{{header}}</th>
            {%- endfor %}
        </tr>
    </thead>
    <tbody>
        {%- for row in properties.rows %}
        <tr{% if properties.selectedRow === loop.index %} aria-selected="true"{% endif %}>
            {%- for item in row %}
            <td>{{item}}</td>
            {%- endfor %}
        </tr>
        {%- endfor %}
    </tbody>
</table>
{%- endmacro %}
