language: node_js
sudo: required
dust: trusty
services:
  - docker
 
before_install:
- docker pull backstopjs/backstopjs:3.8.5
- hostname -I

node_js:
- 8.5

install:
- npm install

script:
- gulp test:visual

branches:
  only:
  - test-travis
  - develop
